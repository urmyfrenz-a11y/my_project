# 실제 값은 .env.local 에 넣으세요 (git 에 커밋되지 않습니다).

# 카카오 장소/리뷰 (필수 — 카카오 REST API 키)
KAKAO_REST_API_KEY=

# 카카오 리뷰 100개+ (선택) — 브라우저 워커(/kakao-worker)를 Render 등에 올리고 연결.
# 없으면 패널 API 기준 ~7개만 수집됩니다. 카카오는 데이터센터 IP를 안 막아 클라우드에서 동작.
KAKAO_WORKER_URL=
KAKAO_WORKER_TOKEN=

# 구글 검색 소스 (선택) — 무료 Serper.dev 키. 없으면 "구글 검색"은 준비중 표시.
SERPER_API_KEY=

# 네이버 (선택) — 별도 Playwright 워커(/naver-worker)를 한국 IP에서 실행 후 연결
NAVER_WORKER_URL=
NAVER_WORKER_TOKEN=
